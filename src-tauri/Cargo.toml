[package]
name = "hello-me-desktop"
version = "0.1.0"
edition = "2021"

[build-dependencies]
tauri-build = "2"

[dependencies]
anyhow = "1"
personal-studio = { path = ".." }
tauri = "2"
tokio = { version = "1", features = ["rt-multi-thread"] }

